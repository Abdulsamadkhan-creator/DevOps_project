name : docker build

on:
 push:
  branches:
    --main

jobs:
 docker-build:
  runs-on: ubuntu-latest

  steps:
   - name : checkout code
     uses : actions/checkout@v4

   - name : build docker image
     uses : docker build -t docker-dashboard .